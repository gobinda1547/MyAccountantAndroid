<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myaccountant.gobinda.cse.ju.org.myaccountant10">

    <!-- for taking picture with the camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".show_account_list_feature.ShowAccountListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".add_account_feature.AddAccountActivity"
            android:label="Add An Account" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".show_account_list_feature.ShowAccountListActivity" />
        </activity>

        <activity
            android:name=".show_account_feature.ShowAccountActivity"
            android:label="Add An Account" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".show_account_list_feature.ShowAccountListActivity" />
        </activity>


        <activity
            android:name=".add_account_feature.TakeImageActivity"
            android:label="Add An Account" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".add_account_feature.AddAccountActivity" />
        </activity>

    </application>

</manifest>